Media Directory: "/var/data/media/video"
Movie Directories: [movies]
Movie Extensions: [mkv, mp4, m4v, avi, mov]
Show Directories: [tv, anime]
Script Path: "/plex_linker"
PUID: 1001
PGID: 1001
# Non-Implemented Variables
# planning to use these two variables to define whether to dig into the movie / show directory for subpaths, my library
# uses subpaths so I built it as such but this feature should be expanded because this is not the standard setup
Show Depth: 1
Movie Depth: 1
